using UnityEngine;

[CreateAssetMenu(fileName = "New Bullet Data", menuName = "Game Design/Bullet Data", order = 1)]
public class BulletData : ScriptableObject
{
    public float speed;
    public float damage;
    // Start is called before the first frame update
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}
